<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Default placeholder category ID
    |--------------------------------------------------------------------------
    |
    | When creating placeholder products (e.g. from PO add-by-supplier-code),
    | this category is used if set. Otherwise, the first leaf category is used.
    |
    */
    'default_placeholder_category_id' => env('PRODUCTS_DEFAULT_PLACEHOLDER_CATEGORY_ID'),
];
